# Copy to ~/.claude/.env (NOT committed). Used by loop.sh and coordinator.

# ntfy topic for notifications (create one at https://ntfy.sh/).
NTFY_TOPIC=your-topic-here

# Optional: dashboard bind address (default 0.0.0.0:8080).
# CLAUDE_DASHBOARD_BIND=0.0.0.0:8080
